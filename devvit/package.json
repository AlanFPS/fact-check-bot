{
  "name": "fact-check-bot-devvit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run typecheck && esbuild src/server/index.ts --bundle --platform=node --format=cjs --packages=external --outfile=dist/server/index.cjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@devvit/web": "^0.13.6",
    "hono": "^4.12.27"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
